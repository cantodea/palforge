diff --git a/emscripten/Makefile b/emscripten/Makefile
index 78c973f..91839e8 100644
--- a/emscripten/Makefile
+++ b/emscripten/Makefile
@@ -22,7 +22,7 @@ SDL_CONFIG = pkg-config sdl3
 override CFLAGS = `$(SDL_CONFIG) --cflags` -gsource-map -Wall -O2 -fno-strict-aliasing -I. -I.. -DUSE_SDL3=1 -I../sdl_compat -I../timidity -DPAL_HAS_PLATFORM_SPECIFIC_UTILS $(DEPFLAGS) $(EMPORTS) $(GENERATED)
 CPPFLAGS = $(CFLAGS) -std=c++11
 override LDFLAGS += `$(SDL_CONFIG) --libs` -gsource-map -O2 $(EMPORTS) --emrun -lidbfs.js -s ASYNCIFY=1 -sEXIT_RUNTIME=1 \
-	-s EXPORTED_FUNCTIONS="['_EMSCRIPTEN_main']" -s 'EXPORTED_RUNTIME_METHODS=["cwrap"]' -sMAX_WEBGL_VERSION=2 -sFULL_ES3 -s TOTAL_STACK=5242880
+	-s EXPORTED_FUNCTIONS="['_EMSCRIPTEN_main','_PALFORGE_SetLaunchTarget']" -s 'EXPORTED_RUNTIME_METHODS=["cwrap"]' -sMAX_WEBGL_VERSION=2 -sFULL_ES3 -s TOTAL_STACK=5242880
 
 POSTCOMPILE = @mv -f $*$(INTER).Td $*$(INTER).d && touch $@
 
diff --git a/game.c b/game.c
index 619c38e..5f00ee1 100644
--- a/game.c
+++ b/game.c
@@ -20,6 +20,7 @@
 //
 
 #include "main.h"
+#include "palforge_runtime.h"
 
 VOID
 PAL_GameMain(
@@ -45,13 +46,24 @@ PAL_GameMain(
    //
    // Show the opening menu.
    //
-   gpGlobals->bCurrentSaveSlot = (BYTE)PAL_OpeningMenu();
-   gpGlobals->fInMainGame = TRUE;
+   if (PALFORGE_HasLaunchTarget())
+   {
+      gpGlobals->bCurrentSaveSlot = 0;
+      gpGlobals->fInMainGame = TRUE;
+      PAL_ReloadInNextTick(0);
+      PAL_LoadResources();
+      PALFORGE_ApplyLaunchTarget();
+   }
+   else
+   {
+      gpGlobals->bCurrentSaveSlot = (BYTE)PAL_OpeningMenu();
+      gpGlobals->fInMainGame = TRUE;
 
-   //
-   // Initialize game data and set the flags to load the game resources.
-   //
-   PAL_ReloadInNextTick(gpGlobals->bCurrentSaveSlot);
+      //
+      // Initialize game data and set the flags to load the game resources.
+      //
+      PAL_ReloadInNextTick(gpGlobals->bCurrentSaveSlot);
+   }
 
    //
    // Run the main game loop.
diff --git a/main.c b/main.c
index c442e8b..77b53fe 100644
--- a/main.c
+++ b/main.c
@@ -20,6 +20,7 @@
 //
 
 #include "main.h"
+#include "palforge_runtime.h"
 #include <setjmp.h>
 
 #if SDL_VERSION_ATLEAST(3,0,0)
@@ -542,8 +543,11 @@ main(
    //
    // Show the trademark screen and splash screen
    //
-   PAL_TrademarkScreen();
-   PAL_SplashScreen();
+   if (!PALFORGE_HasLaunchTarget())
+   {
+      PAL_TrademarkScreen();
+      PAL_SplashScreen();
+   }
 
    //
    // Run the main game routine
diff --git a/play.c b/play.c
index e03389a..033b551 100644
--- a/play.c
+++ b/play.c
@@ -20,6 +20,7 @@
 //
 
 #include "main.h"
+#include "palforge_runtime.h"
 
 VOID
 PAL_GameUpdate(
@@ -73,6 +74,8 @@ PAL_GameUpdate(
 
          PAL_ClearKeyState();
          PAL_MakeScene();
+
+         PALFORGE_RunPendingScript();
       }
 
       //
